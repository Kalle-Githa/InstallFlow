
Kör detta i din PC i appsettings.json filen:
"ConnectionStrings": {
    "DefaultConnection": "Server=ULTRAG\\SQLEXPRESS;Database=InstallFlowDb;Integrated Security=SSPI;TrustServerCertificate=True"
  },
  
Kör detta i din Mac:  
"ConnectionStrings": {
  "DefaultConnection": "Server=localhost,1433;Database=InstallFlowDb;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;"
}

Måste köra detta på macen SQL i den ordningen om man vill rensa tabellerna:
DELETE FROM CartItems;
DELETE FROM Carts;
DELETE FROM ProductCategories;
DELETE FROM Products;
DELETE FROM JobLaborRows;
DELETE FROM JobMaterialRows;
DELETE FROM Jobs;
DELETE FROM Assignments;
DELETE FROM Customers;

och sedan:
dotnet ef database update




   // Temporär debug som läggs i author servicen
        //Console.WriteLine($"Söker efter: '{username}'");
        //Console.WriteLine($"Hittade användare: {user?.Username ?? "NULL"}");
        //Console.WriteLine($"Hash i DB: {user?.PasswordHash}");
        //var verify = BCrypt.Net.BCrypt.Verify(password, user.PasswordHash);
        //Console.WriteLine($"BCrypt.Verify result: {verify}");
        //Console.WriteLine(BCrypt.Net.BCrypt.HashPassword("admin123"));




JobTemplate är inte exponerat alls = fixas efter inlämningsuppgiften



 ===== Seed testanvändare =====
using (var scope = app.Services.CreateScope())
{
    var context = scope.ServiceProvider
        .GetRequiredService<InstallFlowDbContext>();

    if (!context.Users.Any()) 
    {
        context.Users.Add(new User
        {
            Username = "admin",
            PasswordHash = BCrypt.Net.BCrypt.HashPassword("admin123"),
            Role = UserRole.Admin,
            CreatedAt = DateTime.UtcNow
       });

       context.SaveChanges();
   }
}

// Sätts via dotnet user-secrets set eller på en container
//"ConnectionStrings": {
  //  "DefaultConnection": "Server=ULTRAG\\SQLEXPRESS;Database=InstallFlowDb;Integrated Security=SSPI;TrustServerCertificate=True"
  //}, 
  
  // Sätts via dotnet user-secrets set eller på en container
 //"Key": "Superhemliganyckelnsomskavara32lång",
 
 //Sätts via dotnet user-secrets set eller på en container
  "ApplicationInsights": {
     "ConnectionString": "InstrumentationKey=1e2f3e41-68c0-4c7d-87ec-6fc1fec331c4;IngestionEndpoint=https://westeurope-5.in.applicationinsights.azure.com/;LiveEndpoint=https://westeurope.livediagnostics.monitor.azure.com/;ApplicationId=ab5ef13f-b3e4-4aca-ba6c-573c217d468e"
   }


   // Kopia på hur det var innan. Kasta om den ersatta fungerar

   # Stage 1: Bygg applikationen
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src

COPY ["InstallFlow.csproj", "."]
RUN dotnet restore "InstallFlow.csproj"

COPY . .
WORKDIR "/src"
RUN dotnet publish "InstallFlow.csproj" -c Release -o /app/publish

# Stage 2: Kör applikationen
FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS final
WORKDIR /app
COPY --from=build /app/publish .

ENTRYPOINT ["dotnet", "InstallFlow.dll"]